<% content_for :subnav_buttons do %>
    <a data-type="general" class="create_compute btn btn-primary" title="Create New Compute">create</a>
<% end %>

<%= render "computes/list", computes: @computes %>

<% @compute = Compute.new; @compute.set_defaults(current_account) %>
<%= render "computes/dialog", environment: nil, type: "general"  %>
