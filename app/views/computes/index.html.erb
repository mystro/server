<% content_for :subnav_buttons do %>
    <a data-type="compute" data-url="/computes/dialog?environment=&type=general" class="server-dialog btn btn-primary" title="Create New Compute">create</a>
<% end %>

<%= render "computes/list", computes: @computes %>

<%= render "computes/dialog", environment: nil, type: "general"  %>
