<table class="table table-condensed table-striped  tablesorter {sortlist: [[0,0]]}">
  <thead>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Internal</th>
    <th class="links {sorter: false}"></th>
  </tr>
  </thead>
  <tbody>
<% @roles.each do |role| %>
  <tr>
    <td><%= role.name %></td>
    <td><%= role.description %></td>
    <td><%= role.internal %></td>
    <td class="links"><%= render "common/links", :obj => role, :edit => edit_role_path(role) %></td>
  </tr>
<% end %>
  </tbody>
</table>
